
/*
$(function(){

  var grid = $('#grid').get(0);
  var $grid = $(grid.contentDocument);
//  alert($('body', $grid)[0]);

  $('body', $grid).on('load', function() {
    alert('Loaded.');
  });

//  $('header').click(function() {
//    alert($('object#grid html')[0]);
//  });
});
*/

//window.onload = function () {
//  var $grid = $('#grid');
//  var grid = $grid[0].contentDocument;
//  var height = $('body', grid).outerHeight();
////  $grid.css('height', height);
//  $grid.css('height', 2500);
//};
/*
var myobj = {
  prop1: 'hi',
  prop2: 'there'
};

for (var prop in myobj) {
  if (myobj.hasOwnProperty(prop)) {
    console.log(myobj[prop]);
  }
}
*/